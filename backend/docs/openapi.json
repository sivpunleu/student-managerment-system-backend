{
  "openapi": "3.0.3",
  "info": {
    "title": "Student Management API",
    "version": "1.1.0",
    "description": "JWT-authenticated REST API for students, attendance, notes, and tasks."
  },
  "servers": [
    { "url": "http://localhost:3000", "description": "Local development" }
  ],
  "components": {
    "securitySchemes": {
      "bearerAuth": { "type": "http", "scheme": "bearer", "bearerFormat": "JWT" }
    },
    "schemas": {
      "Credentials": {
        "type": "object",
        "required": ["email", "password"],
        "properties": {
          "email": { "type": "string", "format": "email" },
          "password": { "type": "string", "minLength": 8 }
        }
      },
      "Student": {
        "type": "object",
        "required": ["studentId", "fullName", "email", "department"],
        "properties": {
          "studentId": { "type": "string" },
          "fullName": { "type": "string" },
          "email": { "type": "string", "format": "email" },
          "department": { "type": "string" },
          "classGroup": { "type": "string" },
          "gender": { "type": "string", "enum": ["Male", "Female"] },
          "phone": { "type": "string" },
          "year": { "type": "integer", "minimum": 1, "maximum": 6 }
        }
      },
      "CreateStudent": {
        "allOf": [
          { "$ref": "#/components/schemas/Student" },
          {
            "type": "object",
            "properties": {
              "createAccount": { "type": "boolean" },
              "accountPassword": { "type": "string", "minLength": 8 }
            }
          }
        ]
      },
      "ClassGroup": {
        "type": "object",
        "required": ["name"],
        "properties": {
          "name": { "type": "string" },
          "department": { "type": "string" },
          "year": { "type": "integer", "minimum": 1, "maximum": 6 },
          "shift": {
            "type": "string",
            "enum": ["Morning", "Afternoon", "Evening", "Weekend", ""]
          },
          "description": { "type": "string" }
        }
      }
    }
  },
  "paths": {
    "/api/health": {
      "get": {
        "summary": "API health",
        "responses": { "200": { "description": "Healthy" } }
      }
    },
    "/api/auth/register": {
      "post": {
        "summary": "Register a student account",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "allOf": [
                  { "$ref": "#/components/schemas/Credentials" },
                  {
                    "type": "object",
                    "required": ["fullName"],
                    "properties": { "fullName": { "type": "string" } }
                  }
                ]
              }
            }
          }
        },
        "responses": { "201": { "description": "Registered with access and refresh tokens" } }
      }
    },
    "/api/auth/staff": {
      "post": {
        "summary": "Create a staff account (admin)",
        "security": [{ "bearerAuth": [] }],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "allOf": [
                  { "$ref": "#/components/schemas/Credentials" },
                  {
                    "type": "object",
                    "required": ["fullName"],
                    "properties": { "fullName": { "type": "string" } }
                  }
                ]
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Staff account created" },
          "403": { "description": "Admin role required" }
        }
      }
    },
    "/api/auth/login": {
      "post": {
        "summary": "Login",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": { "schema": { "$ref": "#/components/schemas/Credentials" } }
          }
        },
        "responses": { "200": { "description": "Access and refresh tokens" } }
      }
    },
    "/api/auth/refresh": {
      "post": {
        "summary": "Rotate a refresh token",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["refreshToken"],
                "properties": { "refreshToken": { "type": "string" } }
              }
            }
          }
        },
        "responses": { "200": { "description": "Rotated session" } }
      }
    },
    "/api/auth/me": {
      "get": {
        "summary": "Current user",
        "security": [{ "bearerAuth": [] }],
        "responses": { "200": { "description": "Current profile" } }
      }
    },
    "/api/auth/profile": {
      "put": {
        "summary": "Update profile",
        "security": [{ "bearerAuth": [] }],
        "responses": { "200": { "description": "Profile updated" } }
      }
    },
    "/api/auth/profile/avatar": {
      "put": {
        "summary": "Upload a JPEG, PNG, or WebP profile image (maximum 2 MB)",
        "security": [{ "bearerAuth": [] }],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["imageData"],
                "properties": {
                  "imageData": {
                    "type": "string",
                    "format": "byte"
                  }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Profile image updated" },
          "413": { "description": "Image is larger than 2 MB" }
        }
      },
      "delete": {
        "summary": "Remove the current profile image",
        "security": [{ "bearerAuth": [] }],
        "responses": { "200": { "description": "Profile image removed" } }
      }
    },
    "/api/auth/avatar/{userId}": {
      "get": {
        "summary": "Get a user's profile image",
        "parameters": [
          {
            "name": "userId",
            "in": "path",
            "required": true,
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": {
            "description": "Profile image",
            "content": {
              "image/jpeg": {},
              "image/png": {},
              "image/webp": {}
            }
          },
          "404": { "description": "Profile image not found" }
        }
      }
    },
    "/api/auth/change-password": {
      "put": {
        "summary": "Change password and rotate session",
        "security": [{ "bearerAuth": [] }],
        "responses": { "200": { "description": "Password changed" } }
      }
    },
    "/api/auth/forgot-password": {
      "post": {
        "summary": "Request a password reset token",
        "responses": { "200": { "description": "Reset request accepted" } }
      }
    },
    "/api/auth/reset-password": {
      "post": {
        "summary": "Reset password with a reset token",
        "responses": { "200": { "description": "Password reset and new session issued" } }
      }
    },
    "/api/auth/logout": {
      "post": {
        "summary": "Revoke the current session",
        "security": [{ "bearerAuth": [] }],
        "responses": { "200": { "description": "Session revoked" } }
      }
    },
    "/api/users": {
      "get": {
        "summary": "List user accounts (admin)",
        "security": [{ "bearerAuth": [] }],
        "parameters": [
          {
            "name": "role",
            "in": "query",
            "schema": {
              "type": "string",
              "enum": ["admin", "staff", "student"]
            }
          },
          {
            "name": "status",
            "in": "query",
            "schema": {
              "type": "string",
              "enum": ["active", "inactive"]
            }
          },
          {
            "name": "search",
            "in": "query",
            "schema": { "type": "string" }
          }
        ],
        "responses": {
          "200": { "description": "Paginated user accounts with linked student profiles" },
          "403": { "description": "Admin role required" }
        }
      }
    },
    "/api/users/{id}/role": {
      "patch": {
        "summary": "Update a user role (admin)",
        "security": [{ "bearerAuth": [] }],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": { "type": "string" }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["role"],
                "properties": {
                  "role": {
                    "type": "string",
                    "enum": ["admin", "staff", "student"]
                  }
                }
              }
            }
          }
        },
        "responses": { "200": { "description": "User role updated" } }
      }
    },
    "/api/users/{id}/status": {
      "patch": {
        "summary": "Activate or deactivate a user account (admin)",
        "security": [{ "bearerAuth": [] }],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": { "type": "string" }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["isActive"],
                "properties": {
                  "isActive": { "type": "boolean" }
                }
              }
            }
          }
        },
        "responses": { "200": { "description": "User status updated" } }
      }
    },
    "/api/users/{id}/reset-password": {
      "post": {
        "summary": "Reset a user password (admin)",
        "security": [{ "bearerAuth": [] }],
        "parameters": [
          {
            "name": "id",
            "in": "path",
            "required": true,
            "schema": { "type": "string" }
          }
        ],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["newPassword"],
                "properties": {
                  "newPassword": { "type": "string", "minLength": 8 }
                }
              }
            }
          }
        },
        "responses": { "200": { "description": "Password reset successfully" } }
      }
    },
    "/api/students": {
      "get": {
        "summary": "List students",
        "security": [{ "bearerAuth": [] }],
        "responses": { "200": { "description": "Paginated students" } }
      },
      "post": {
        "summary": "Create student (admin)",
        "security": [{ "bearerAuth": [] }],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": { "schema": { "$ref": "#/components/schemas/CreateStudent" } }
          }
        },
        "responses": { "201": { "description": "Student and optional login account created" } }
      }
    },
    "/api/departments": {
      "get": {
        "summary": "List departments",
        "security": [{ "bearerAuth": [] }],
        "responses": { "200": { "description": "Paginated departments" } }
      }
    },
    "/api/classes": {
      "get": {
        "summary": "List classes",
        "security": [{ "bearerAuth": [] }],
        "responses": { "200": { "description": "Paginated classes" } }
      },
      "post": {
        "summary": "Create class (admin)",
        "security": [{ "bearerAuth": [] }],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": { "schema": { "$ref": "#/components/schemas/ClassGroup" } }
          }
        },
        "responses": { "201": { "description": "Class created" } }
      }
    },
    "/api/attendances": {
      "get": {
        "summary": "List attendance",
        "security": [{ "bearerAuth": [] }],
        "parameters": [
          {
            "name": "classGroup",
            "in": "query",
            "schema": { "type": "string" },
            "description": "Limit records to students in this class/group"
          }
        ],
        "responses": { "200": { "description": "Attendance records" } }
      }
    },
    "/api/attendances/summary": {
      "get": {
        "summary": "Daily attendance summary",
        "security": [{ "bearerAuth": [] }],
        "parameters": [
          {
            "name": "classGroup",
            "in": "query",
            "schema": { "type": "string" },
            "description": "Limit summary to students in this class/group"
          }
        ],
        "responses": { "200": { "description": "Daily summary" } }
      }
    },
    "/api/attendances/report": {
      "get": {
        "summary": "Attendance date-range report",
        "security": [{ "bearerAuth": [] }],
        "parameters": [
          {
            "name": "classGroup",
            "in": "query",
            "schema": { "type": "string" },
            "description": "Limit report to students in this class/group"
          }
        ],
        "responses": { "200": { "description": "Daily report rows and totals" } }
      }
    },
    "/api/notes": {
      "get": {
        "summary": "List personal notes",
        "security": [{ "bearerAuth": [] }],
        "responses": { "200": { "description": "Notes" } }
      },
      "post": {
        "summary": "Create note",
        "security": [{ "bearerAuth": [] }],
        "responses": { "201": { "description": "Note created" } }
      }
    },
    "/api/tasks": {
      "get": {
        "summary": "List personal tasks",
        "security": [{ "bearerAuth": [] }],
        "responses": { "200": { "description": "Tasks" } }
      },
      "post": {
        "summary": "Create task",
        "security": [{ "bearerAuth": [] }],
        "responses": { "201": { "description": "Task created" } }
      }
    }
  }
}
